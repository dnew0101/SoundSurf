export const SpeedLines = () => {

    return
    <div>
        This will be dotted lines on the track that follow the tracks contour
        Likely passed in as props from the race track.
    </div>
}